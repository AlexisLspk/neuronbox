{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/AlexisLspk/neuronbox/main/specs/swap-signal.schema.json",
  "title": "swap_signal.json",
  "description": "Written by neurond to ~/.neuronbox/swap_signal.json when the client sends SwapModel. Read by serve_worker.py (neuron serve).",
  "type": "object",
  "required": ["signal_version", "model_ref", "ts"],
  "properties": {
    "signal_version": {
      "type": "integer",
      "description": "Format version; bump on incompatible changes.",
      "minimum": 1
    },
    "model_ref": {
      "type": "string",
      "description": "Hugging Face id or logical reference passed to neuron swap."
    },
    "quantization": {
      "type": ["string", "null"],
      "description": "Optional, e.g. q4_k_m."
    },
    "ts": {
      "type": "integer",
      "description": "Unix timestamp (seconds) when the file was written."
    }
  },
  "additionalProperties": true
}
