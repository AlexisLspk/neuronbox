[project]
name = "neuronbox"
version = "0.1.0"
description = "Optional Python client for the NeuronBox Unix-socket daemon"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
